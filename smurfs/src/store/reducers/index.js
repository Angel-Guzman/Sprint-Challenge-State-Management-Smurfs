import { FETCH_SMURF_START }  from '../actions';

export const reducer = (state, action) => {
    switch(action.type) {
        case FETCH_SMURF_START:
        default: 
            return state;
    }
}